/**
 * Skill type enumeration
 * Defines the types of language skills that can be tested
 */
export enum SkillType {
  SPEAKING = "speaking",
  WRITING = "writing",
}

/**
 * Difficulty level enumeration
 * Defines the difficulty levels for language prompts
 */
export enum DifficultyLevel {
  EASY = "easy",
  MEDIUM = "medium",
  HARD = "hard",
}
