/**
 * Attempt status enumeration
 * Defines the different states an attempt can be in
 */
export enum AttemptStatus {
  IN_PROGRESS = "in_progress",
  SUBMITTED = "submitted",
  SCORED = "scored",
}
